<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <link rel="stylesheet" href="/css/index.css?v={% version %}"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href='https://unpkg.com/boxicons@2.1.2/css/boxicons.min.css' rel='stylesheet'>
  <title>{{ title }} | {{ site.name }}</title>
</head>
<body class=" font-mono bg-slate-50 min-h-screen text-gray-900 text-xl lg:text-2xl">
  <header class=" p-10">
    <div class=" relative">
      <h1 class=" absolute -z-10 invisible">{{ site.name }}</h1>
      <figure>
        <img class=" mx-auto max-w-xs w-full" src="/assets/hero.jpg" alt="{{ site.name }}">
      </figure>
    </div>
  </header>
  <nav class=" p-10">
    <ul class=" text-center">
      {%- for item in category -%}
      <a class=" hover:text-amber-400 inline-block mx-4"
        href="{{ item.url }}"
      >
        <li>{{ item.name }}</li>
      </a>
      {%- endfor -%}
    </ul>
  </nav>
  <main>
    {{ content | safe }}
  </main>
  {% include 'components/footer.njk' %}
</body>
</html>